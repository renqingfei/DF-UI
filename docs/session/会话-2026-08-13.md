# 会话记录 · 2026-08-13

> 项目：DF UI · 三端 Vue 3 组件库
> 会话 ID：`140014-a647bd87`（14:00–15:52）→ 接续 `170838-d9b55ec8`（17:08 起）
> 仓库：`C:\Users\rqf\Desktop\DF_UI`（AI 工作区 `d:\xingyun-zuinew`）

---

## 背景

用户想做一套 Vue 组件库，类似 Element 那样"什么都有"（明确点名要骨架屏），
同时要兼容移动端和 uniapp。用户**不懂前端**，需要由 AI 主导收集需求、给出方案、承担实现。

要求先在 `C:\Users\rqf\Desktop\DF_UI` 建 git 仓库。

---

## 已确认的决策

| # | 决策点 | 结论 | 确认时间 |
| --- | --- | --- | --- |
| 1 | 仓库位置 | `C:\Users\rqf\Desktop\DF_UI`，git 已初始化（分支 master） | 14:05 |
| 2 | 覆盖范围 | **三个版本都要**：PC 后台 / 手机 H5 / uniapp 小程序 | 14:12 |
| 3 | 组织方式 | monorepo 单仓库，对外发 3 个包，对内共享 tokens + core + API 契约 | 14:12 |
| 4 | 第一批范围 | 28 个组件（见 `02-组件清单-第一批.md`） | 14:20 |
| 5 | 第二批范围 | 20 个组件（见 `03-组件清单-第二批.md`） | 14:20 |
| 6 | 视觉风格（第一次否决） | 用户否掉 A/B/C/D 四种传统风格："和 Element 没啥区别" | 14:40 |
| 7 | 视觉风格（第二次否决） | 用户否掉 iOS 26 Liquid Glass："没有苹果设备上好看"，升级壁纸+桌面模拟后仍不满意，明确表示**不要模仿苹果，要自己的风格** | 15:20 |
| 8 | **方法改变** | 停止"猜一次做一次"，改为**风格实验室**：一次给 8 个差异极大的原创方向并排对比，由用户排除法收敛 | 15:25 |
| 9 | **风格方向收敛** | 用户从 8 个方向中选中 **02 暗夜霓虹 / 03 黏土软糖 / 04 便当格 / 07 莫兰迪柔雾** 四个 | 15:50 |
| 10 | 合成方案（已作废） | 曾尝试把四者合成一套（04 骨架 + 03 触感 + 07 浅色 + 02 暗色），做出 `df-style.html` | 15:55 |
| 11 | **最终决策** | 用户明确：**不合成，四个原样保留为四套内置主题，配色一点不改**。<br>组件代码一份，主题 = 一份令牌配置，`data-theme` 切换 | 16:10 |
| 12 | **默认主题** | **`clay` 黏土软糖** | 16:30 |
| 13 | 移动端返工 | 用户反馈手机端"有点小气"。原因：直接沿用 PC 间距字号、缺主视觉重心、模拟框偏小。<br>已重做：手机框放大到接近真机比例、主色大卡当视觉锚点、补问候栏 / 快捷宫格 / 状态时间轴 / 数据三栏 / 底部操作栏 | 16:40 |
| 14 | 第一批组件调整 | 因移动端版式需要，追加 `Grid` 宫格与 `ActionBar` 底部操作栏，**第一批 28 → 30 个** | 16:40 |
| 15 | 工程地基落地 | pnpm workspace monorepo 建成：`tokens` / `core` / `pc` / `playground-pc` / `scripts`。TypeScript + Vite 8 + Vitest 4 | 17:00 |
| 16 | 令牌固化 | 四套主题写成 TS 令牌文件，**每套 47 个令牌、key 强制一致**，由 `themes:validate` 脚本守门 | 17:00 |
| 17 | 首个组件跑通 | `DfButton` 完成：5 type × 4 variant × 3 size × 3 shape + block/loading/disabled/icon 插槽；**35 个单测全绿** | 17:10 |
| 18 | 换肤链路验证 | 真实浏览器验证四套主题运行时切换：`data-theme` 切换后 CSS 变量、按钮底色、页面底色全部正确响应，组件代码零颜色耦合 | 17:20 |
| 19 | **首次 git 提交** | 用户拍板「先提交，再按 A→B→C→D 走」。root-commit `2f1c1d3`，52 文件 / 10567 行 | 17:30 |
| 20 | **API 契约上移** | props / emits 从 `packages/pc/` 提到 `packages/core/src/props/`，三端取同一份；pc 原路径退化为转发文件 | 17:35 |
| 21 | **逻辑层复用** | 类名推导与禁用判定抽成 `core/composables/use-button.ts`，三端模板只剩「结构 + 样式」 | 17:35 |
| 22 | **Button 补齐三端** | 新增 `@df-ui/h5`（触屏版）与 `@df-ui/uni`（小程序版，源码分发） | 17:40 |
| 23 | 移动端令牌 | 新增 `m-control-h-sm/md/lg`，四套主题各自实现，令牌 47 → **50** 个 | 17:40 |
| 24 | uni 单位规则修订 | 触控尺寸改用 px 不用 rpx（rpx 会在小屏缩到 44pt 以下）。已改写 `01-架构方案.md` 第九节 | 17:40 |
| 25 | H5 预览工程 | 新增 `playground/h5`（端口 5181），PC 预览页用手机框 iframe 嵌进来，主题经 postMessage 同步 | 17:45 |
| 26 | 修掉 typecheck | `pnpm typecheck` 原本根本跑不起来（vue-tsc 3.3.9 要 `typescript/lib/tsc`，TS 7 已移除）。TS 回落 5.9.3 并补 `@types/node` 后全绿 | 17:50 |
| 27 | **文档站落地** | VitePress 1.6.4：示例容器 + 主题切换器 + 手机框预览，Button 页含 6 个活示例与完整 API 表 | 18:05 |
| 28 | 样式入口拆分 | pc 增 `style/components`（不含 base）、h5 增 `style/scoped`（规则包在 `.df-h5-scope` 下），解决「同页同时展示两端」的类名撞车 | 18:05 |
| 29 | uni 兜底值对齐 | 小程序降级用的写死值原本是随手写的，现已逐个对齐默认主题 clay，并加测试比对 | 18:10 |
| 30 | 用户拍板批量推进 | 「先做 Input 和 Form 这两个硬的，后边的直接全量做，我不看了，做完最后统一看」 | 18:15 |
| 31 | **波 1** | 表单校验引擎 + Input / Textarea / Form / FormItem 三端 | 18:35 |
| 32 | **波 2** | Checkbox（含 Group）/ Radio（含 Group）/ Switch 三端 | 18:50 |
| 33 | **波 3** | Skeleton / Card / Tag / Badge / Avatar / Empty / Divider / Space 三端 | 19:05 |
| 34 | 文档站扩充 | 新增 6 个组件页，示例代码改用 VitePress 的 `<<< @/path` 文件引入，**带语法高亮且与实际示例同源** | 19:15 |

---

## 待用户拍板

| # | 待定项 | 候选 | 状态 |
| --- | --- | --- | --- |
| 1 | 是否做首次 git commit | 仓库至今 0 提交，全部文件仍是未跟踪状态 | **待确认（本轮再次提出）** |
| 2 | 下一批组件顺序 | 建议 Input → Form → Skeleton → Card/Tag/Empty | 待确认 |
| 3 | 库名与前缀 | 默认 DF UI + `df-` 前缀 | 用户未反对，暂按默认 |
| 4 | 是否发 npm | 默认先自用，架构按可发布标准做 | 暂按默认 |
| 5 | 组件清单增删 | 用户看完清单后可提 | 未反馈 |
| ~~6~~ | ~~玻璃四参数 / iOS 主色 / A-D 传统预设~~ | ~~—~~ | **已作废**：iOS 玻璃与 A/B/C/D 均被用户否决，最终方案是四套内置主题 |

---

## 本轮产出

| 文件 | 内容 |
| --- | --- |
| `README.md` | 项目总览、仓库结构、文档索引、开发路线 |
| `.gitignore` | Node / Vite / uniapp / 编辑器 忽略规则 |
| `docs/planning/01-架构方案.md` | 三端复用分层、分包职责、API 对齐原则、样式方案、构建发布、风险预案 |
| `docs/planning/02-组件清单-第一批.md` | 28 个组件的用途、三端支持矩阵、props/events/slots、平台差异 |
| `docs/planning/03-组件清单-第二批.md` | 20 个进阶组件 + 18 个候补池组件 |
| `docs/planning/04-设计风格与主题规范.md` | 四风格参数对照、色阶推导算法、中性色、尺寸/动效/层级令牌、暗色主题 |
| `preview/style-preview.html` | 可交互风格预览页：四风格并排对比、实时换主色（含 10 级色阶推导）、明暗切换、骨架屏三种动画 |
| `docs/planning/05-液态玻璃设计规范.md` | **默认风格规范**：玻璃三层结构与核心 CSS、五级材质、可调令牌、iOS 系统色板、形状动效、三端降级矩阵、性能约束、无障碍 |
| `preview/ios26-liquid-glass.html` | 液态玻璃版（用户已否决，保留备查）：组件全家桶 + PC 后台整页 + iOS 26 桌面/控制中心/锁屏模拟 + 六套湍流流体壁纸 |
| `preview/style-lab.html` | 风格实验室：8 个原创方向（粗野 / 霓虹 / 黏土 / 便当 / 杂志 / 终端 / 莫兰迪 / 流光），同一界面渲染 8 遍并排对比 |
| `preview/df-style.html` | 合成版（已作废，保留备查）：02+03+04+07 合成，可切「浅色/暗色」「柔/净/静」「六个低饱和主色」 |
| `preview/style-4.html` | 四个原版方向的放大对比页，标注各自在合成版里的贡献 |
| `preview/df-themes.html` | **当前主预览页 · 四套内置主题**：neon / clay / bento / muted 原样保留，顶部一键切换，每套都含 PC 后台整页 + 组件全家桶 + 三个手机页 |
| `docs/planning/06-主题体系规范.md` | **定稿设计文档**：四套主题的完整令牌表、特有规则、跨主题一致性约束、使用方式、三端适配与小程序包体优化 |

### 工程产出（17:00 起）

| 文件 / 目录 | 内容 |
| --- | --- |
| `package.json` · `pnpm-workspace.yaml` · `tsconfig*.json` · `vitest.config.ts` | monorepo 根配置：workspace 划分、TS 路径映射、测试环境（happy-dom） |
| `packages/tokens/src/types.ts` | `DfTheme` 与 `DfTokenKey` 类型：**令牌 key 由类型系统锁死**，漏写一个直接编译报错 |
| `packages/tokens/src/themes/{neon,clay,bento,muted}.ts` | 四套主题令牌，各 47 项（颜色 / 圆角 / 阴影 / 控件高 / 字体 / 动效 / 移动端专属尺寸） |
| `packages/tokens/src/index.ts` | 主题注册表、`DEFAULT_THEME = 'clay'`、`getTheme` / `isThemeKey` / `themeList` |
| `packages/core/src/theme.ts` | 换肤运行时：`setTheme` / `useTheme` / `currentTheme` + **`ThemeAdapter` 适配层**（Web 改 `<html data-theme>`；小程序由 uni 包注入自己的实现） |
| `packages/core/src/bem.ts` | BEM 类名生成器 `b()` / `b.m()` / `b.is()`，三端共用，保证类名结构一致 |
| `packages/core/src/install.ts` | 组件注册助手（`withInstall`），支持全量注册与按需引入 |
| `packages/pc/src/components/button/src/button.ts` | **props / emits 单独成文件**，三端 import 同一份，从根上防止 API 走偏 |
| `packages/pc/src/components/button/src/button.vue` | Button 实现：loading 时拦截 click、`aria-busy`、icon 与默认插槽分离 |
| `packages/pc/src/components/button/style/index.scss` | Button 样式：**全部颜色走 CSS 变量，零硬编码色值** |
| `scripts/validate-themes.ts` | 令牌一致性校验：四套主题 key 必须完全相同、不允许空值。`pnpm themes:validate` |
| `scripts/build-theme-css.ts` | 从 TS 令牌生成 `[data-theme=x]` CSS 变量表 |
| `playground/pc/` | 真组件预览工程（Vite，端口 5180）：顶部四主题一键切换，Button 全部形态可点 |
| `packages/*/__tests__/` | 3 个测试文件、35 个用例：令牌一致性、主题切换、Button 行为与类名 |

---

## 关键技术判断（供后续复用）

1. **网页与小程序的界面层无法复用**——小程序无 DOM、标签体系不同、无 Teleport、样式选择器受限。
   强行复用会三端都做不好。解法是分层：令牌 100% 共享 / 逻辑 core 约 90% 共享 / 界面层各写各的，
   总工作量约 1.7 套。
2. **`@df-ui/uni` 必须源码分发**，不能预打包——小程序需要 uni-app 编译器处理 `.vue` 源码，
   走 `uni_modules` + easycom 规范。
3. **props 定义抽到独立文件**（`button.ts` 与 `button.vue` 分离），三端 import 同一份，
   从根本上防止 API 走偏。
4. **小程序 CSS 变量兼容性**需双轨：SCSS 编译期写死默认值兜底 + CSS 变量运行时覆盖换肤。
5. **色阶推导**采用 HSV 空间的 Ant Design 色板算法（色相偏移 + 饱和度/明度分段步进），
   已在预览页实现并验证。
6. **液态玻璃 = 三层叠加**：`backdrop-filter: blur + saturate`（底）× `rgba` 半透明本体（中）×
   渐变边框环 + inset 高光（顶）。缺第三层就只是磨砂塑料，不是玻璃。
7. **`backdrop-filter: url(#svgFilter)` Chrome 已验证支持**，可做真实折射位移；
   视觉增益有限、成本高，默认关闭。
8. **玻璃最大的落地风险在小程序安卓端**：部分机型不支持 backdrop-filter 或严重掉帧。
   降级方案必须在动手前定：玻璃→高不透明度纯色，但边缘高光、圆角、阴影、动效全部保留，
   保证降级后仍是同一套设计语言。
9. **玻璃只给"框架层"用**（侧边栏、导航、卡片外壳、按钮），
   表格正文与长表单一律用 `solid` 材质，否则久看伤眼。这条写进组件默认值。

---

### 工程产出（17:30 起 · 三端打通）

| 文件 / 目录 | 内容 |
| --- | --- |
| `packages/core/src/props/button.ts` | **三端唯一的 API 契约**：props / emits / 取值枚举。不许 import 任何 DOM API |
| `packages/core/src/composables/use-button.ts` | 三端唯一的类名推导与禁用判定；事件对象的 `preventDefault` 留给各端处理（小程序事件对象没这方法） |
| `packages/pc/.../src/button.ts` | 退化为转发文件，只为保持 `@df-ui/pc` 导出路径不变 |
| `packages/h5/` | `@df-ui/h5` 全新包：触屏版 Button + 移动端 base 样式 |
| `packages/uni/` | `@df-ui/uni` 全新包：`view`/`text` 搭的 Button、`df-config-provider` 主题根容器、不依赖 document 的主题适配器 |
| `packages/tokens/` | 新增 `m-control-h-sm/md/lg`，四套主题各自实现，共 50 个令牌 |
| `__tests__/button-api-parity.test.ts` | **跨端一致性闸门**：断言三端拿到同一个 props 对象、同属性生成同类名、同样拦点击 |
| `playground/h5/` | H5 预览工程（端口 5181，开 host 便于真机访问） |
| `playground/pc/src/App.vue` | 底部新增手机框对照区，iframe 嵌 H5 工程，主题经 postMessage 同步 |

### 三端差异实录（Button）

| 维度 | pc | h5 | uni |
| --- | --- | --- | --- |
| 根节点 | `<button>` | `<button>` | `<view>`（小程序原生 button 样式改不动） |
| 触发事件 | `@click` | `@click` | `@tap`，对外仍抛 `click` |
| 控件高度 | `control-h-*`（clay 44px） | `m-control-h-*`（clay 48px） | 同 h5，且不用 rpx |
| 按下反馈 | CSS `:active` + hover | 只有 `:active`，hover 关进 `@media (hover:hover)` | 小程序原生 `hover-class`，视图层直接处理 |
| 换肤落点 | `<html data-theme>` | 同 pc | `df-config-provider` 绑内联 CSS 变量 |
| CSS 变量 | 直接用 | 直接用 | 每个都带写死兜底值，低版本小程序也有样式 |
| 已知差异 | — | — | `nativeType` 无效（view 实现，表单提交要用原生 button） |

### 工程产出（18:00 起 · 文档站）

| 文件 / 目录 | 内容 |
| --- | --- |
| `docs/.vitepress/config.ts` | 站点配置、侧边栏、源码别名（文档站直吃源码，改组件立刻能看到效果）。`srcExclude` 把 planning / session 排除在站外 |
| `docs/.vitepress/theme/index.ts` | 注册 PC 组件、注入主题变量、加载两端样式、注册三个文档专用组件 |
| `.../components/DemoBlock.vue` | 示例容器：预览区 + 「查看代码」折叠。源码用 `?raw` 从示例文件读，**代码与预览永远同源，不会写着一套演着另一套** |
| `.../components/ThemeSwitch.vue` | 主题切换器，色卡左半品牌色右半页面底色 |
| `.../components/PhonePreview.vue` | 手机框，内含 `.df-h5-scope` 作用域 |
| `docs/.vitepress/demos/pc/*.vue` | 5 个 PC 示例（类型 / 填充 / 尺寸形状 / 加载禁用 / 图标） |
| `docs/.vitepress/demos/h5/*.vue` | H5 示例，局部 import `@df-ui/h5`（全局注册的是 PC 版） |
| `docs/index.md` | 首页 |
| `docs/guide/{introduction,install,theme,platforms}.md` | 指南四篇：这是什么 / 安装使用 / 四套主题 / 三端差异 |
| `docs/components/button.md` | Button 文档：6 个活示例 + 属性 / 事件 / 插槽 / 组件级令牌四张表 + 平台差异警告 |
| `packages/pc/src/style/components.scss` | 只含组件样式的入口，不接管全局 html / body |
| `packages/h5/src/style/scoped.scss` | 作用域版入口，规则包在 `.df-h5-scope` 下 |
| `packages/h5/.../style/_rules.scss` | 规则收进 mixin，同时产出根版与作用域版两个入口 |

### 组件产出清单（截至 19:15）

| 组件 | pc | h5 | uni | 说明 |
| --- | :-: | :-: | :-: | --- |
| Button | ✅ | ✅ | ✅ | 5 类型 × 4 填充 × 3 尺寸 × 3 形状 |
| Input | ✅ | ✅ | ✅ | 清空 / 密码显隐 / 字数统计 / 前后缀 |
| Textarea | ✅ | ✅ | ✅ | 自动增高 / 字数统计 |
| Form + FormItem | ✅ | ✅ | ✅ | 校验引擎、三层联动、reset / clear |
| Checkbox + Group | ✅ | ✅ | ✅ | 半选、max / min 限制 |
| Radio + Group | ✅ | ✅ | ✅ | 圆点 + button 分段控件 |
| Switch | ✅ | ✅ | ✅ | 自定义值、异步 beforeChange |
| Skeleton + Item | ✅ | ✅ | ✅ | 5 版式 + 自由拼装 + 3 动画 |
| Card | ✅ | ✅ | ✅ | 头部 / 附加 / 页脚 |
| Tag | ✅ | ✅ | ✅ | 6 语义 × 3 填充，可关闭 |
| Badge | ✅ | ✅ | ✅ | max 溢出、dot、0 不显示 |
| Avatar | ✅ | ✅ | ✅ | 加载失败退回文字 |
| Empty | ✅ | ✅ | ✅ | 3 种插画，纯 CSS / SVG |
| Divider | ✅ | ✅ | ✅ | 带文字、虚线、竖向 |
| Space | ✅ | ✅ | — | 小程序 gap 支持不全，不提供 |

**16 个组件（含 4 个 Group / Item 子组件），第一批 30 个里已完成 15 个主组件。**

## 进度对照

| 步骤 | 状态 |
| --- | --- |
| 1. 定出默认主题 | ✅ `clay` 黏土软糖 |
| 2. 四份令牌文件 + key 一致性校验脚本 | ✅ 4 × 50 令牌，校验通过 |
| 3. monorepo 地基（pnpm workspace + Vite + Vitest） | ✅ 已跑通（turborepo 尚未接入，组件多了再说） |
| 4. Button 打通全链路 | ✅ **三端全通**：pc / h5 / uni + 四主题 + 两个预览工程 |
| 5. 首次 git 提交 | ✅ `2f1c1d3` |
| 6. VitePress 文档站 | ✅ 7 个组件页 + 4 篇指南，`docs:build` SSR 通过 |
| 7. 批量做组件 | 🟡 15 / 30 个主组件完成，测试 212 个全绿 |

## 已知缺口（下次接手先看这里）

| 缺口 | 影响 | 说明 |
| --- | --- | --- |
| **各包没有 vite.config.ts 与 tsconfig.build.json** | `pnpm build` 现在跑不了 | 包内 `build` 脚本引用了这两个文件但从未创建过。发 npm 之前必须补。文档站与预览工程直接吃源码，所以不影响日常开发 |
| uni 端没有真机/模拟器验证 | 小程序里的实际表现未经证实 | 本机没有 HBuilderX / uni CLI 工程。目前 uni 端只有单测 + 源码约束测试守着 |
| Button 页的「查看代码」无语法高亮 | 只有那一页是纯文本 | 后来的 6 个组件页改用 VitePress 的 `<<< @/path` 文件引入，**既同源又有高亮**。Button 页还是老的 `?raw` 折叠方式，回头统一过来 |
| 预览工程只有 Button | 新组件只能在文档站看 | `playground/` 两个工程还是 Button 专用页。文档站已经覆盖全部组件，预览工程现在的作用退化成「真组件 + 换肤」的冒烟测试 |
| 没有 turborepo / changesets / lint | 多包构建无缓存、发版靠手工 | 组件数量上去之后再补，现在不划算 |
| Sass legacy JS API 弃用警告 | 构建时刷警告，功能无影响 | VitePress 1.6.4 内部用的老 API，等它升级 |

## 下一步

### 还没做的组件（第一批剩 15 个）

| 分组 | 组件 | 卡在哪 / 要先解决什么 |
| --- | --- | --- |
| 反馈（5） | Toast / Loading / Dialog / Popup / Message | **都要先有一层「浮层基建」**：层级栈管理、遮罩、进出场动画、滚动锁定。小程序还没有 Teleport，得走 `root-portal` / `cover-view`，这是一次独立的架构设计，不适合塞在组件里顺手做 |
| 导航（4） | Tabs / Menu-TabBar / NavBar-Pagination / ActionBar | Tabs 的滑动指示器要测量元素宽度，小程序端得走异步测量接口 —— core 里那个接口还没写 |
| 表单（1） | Select / Picker | 依赖 Popup（PC 下拉、移动端底部滚轮） |
| 展示（3） | Table / List / Grid | Table 只做 PC（固定表头、固定列）；List / Grid 只做移动端 |
| 基础（2） | Icon / Layout(Row-Col) | Icon 要先定图标来源（自绘还是接开源图标集），这是个待用户拍板的选项 |

### 按顺序建议

1. **先做浮层基建 + Toast / Loading / Dialog / Popup** —— 一次把浮层这层地基打好，
   后面 Select、Menu、ActionSheet 全都吃这一份
2. Tabs 之前先在 core 里补「异步测量」接口，三端各写适配器
3. Icon 需要用户先定图标来源
4. 补各包构建配置，让 `pnpm build` 真能出产物
5. 找机会用 HBuilderX 起一个 uni 工程，把 uni 端在真机上验一次

## 验证记录

### 17:10–17:25（接手核对）

| 验证项 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 令牌一致性 | `pnpm themes:validate` | 通过：4 套 × 47 令牌，key 完全一致，无空值 |
| 单元测试 | `pnpm test` | 3 文件 / 35 用例全绿，2.53s |
| 预览工程启动 | `pnpm dev` | Vite 8.2.1，`http://localhost:5180/`，1.4s 就绪 |
| 四主题真机换肤 | 浏览器实测点击切换 + 读 `getComputedStyle` | 四套全部正确：`data-theme` → CSS 变量 → 按钮底色 / 页面底色逐级生效 |

### 17:30–17:55（三端打通）

| 验证项 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 令牌一致性 | `pnpm themes:validate` | 通过：4 套 × **50** 令牌 |
| 单元测试 | `pnpm test` | **6 文件 / 59 用例全绿**（含三端 API 一致性 6 项） |
| 类型检查 | `pnpm typecheck` | 全绿（修好工具链之后第一次真正跑通） |
| 两端预览同时启动 | `pnpm dev` | pc 5180 / h5 5181 并行，均正常 |
| 主题跨 iframe 同步 | 在 PC 页切主题，看手机框 | 生效，跨源 postMessage 正常 |
| 移动端触控高度 | 浏览器实测 `getBoundingClientRect` | clay 主题下 small 40 / medium 48 / large 56 / block 56 px，全部 ≥ 44 的底线要求 |
| PC 与 H5 高度差异 | 同上对照 | PC medium 44px vs H5 medium 48px —— 确认没有复用桌面尺寸 |

### 18:00–18:15（文档站）

| 验证项 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 单元测试 | `pnpm test` | 6 文件 / **60 用例**全绿（新增 uni 兜底值比对） |
| 类型检查 | `pnpm typecheck` | 全绿，且已把文档站源码纳入检查范围 |
| 文档站开发态 | `pnpm docs:dev` | `http://localhost:5174/`（5173 被占） |
| 文档站构建 | `pnpm docs:build` | 通过，SSR 渲染无报错，7.25s |
| 两端同页共存 | 浏览器实测同一页面取两端按钮 | PC 44px / 圆角 16px，H5 48px / 圆角 24px，**互不污染**，作用域方案有效 |
| 文档站换肤 | 点主题切换器 | 示例舞台与手机框整套换肤，VitePress 自身外壳不受影响 |

### 18:15–19:20（批量做组件）

| 验证项 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 单元测试 | `pnpm test` | 11 文件 / **212 用例**全绿 |
| 类型检查 | `pnpm typecheck` | 全绿 |
| 令牌校验 | `pnpm themes:validate` | 通过，4 × 50 |
| 文档站构建 | `pnpm docs:build` | 通过，8.24s |
| 表单校验真机实测 | 浏览器点文档站「提交」 | 四条必填提示按预期出现，必填星号 2 个，异步查重可用 |
| 骨架屏真机实测 | 浏览器打开 skeleton 页 | 6 个骨架实例、30 个占位块，切动画与切真内容都正常 |

**踩坑记录**：一个包里 14 个组件样式入口都叫 `index.scss`，一次 `@use` 进来会报
「There's already a module with namespace "index"」。Sass 用文件名当默认命名空间，
必须逐个 `as button` / `as input` 显式取别名。

> **踩坑记录（省下次的时间）**：用自动化在**后台标签页**截图验证换肤时，后台页被浏览器节流、不推进帧，
> CSS transition 会停在中间态，截出来的颜色是两套主题的混色，看着像 bug。
> 判定换肤是否真的生效，要读 `getComputedStyle` 的实际值，别只信截图。

---

## 需求收敛过程的教训（供以后复用）

用户不懂前端、也说不清想要什么时，**"猜一个做一个"效率极低**（本次连废两版）。
有效的做法是：**一次性给多个差异极大的方向并排对比，让用户用排除法收敛**。
本次 8 个方向一轮就锁定了 4 个，再一轮就定了最终方案。
