import { withInstall } from '@df-ui/core'
import Empty from './src/empty.vue'

export const DfEmpty = withInstall(Empty)

export { emptyProps, emptyImages, type EmptyProps, type EmptyImage } from '@df-ui/core'
