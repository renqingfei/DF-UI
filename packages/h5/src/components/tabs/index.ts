import { withInstall } from '@df-ui/core'
import Tabs from './src/tabs.vue'

export const DfTabs = withInstall(Tabs)

export { tabsProps, tabsEmits, tabsTypes, type TabsProps, type TabsType, type TabItem } from '@df-ui/core'
