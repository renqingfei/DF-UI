import { withInstall } from '@df-ui/core'
import Pagination from './src/pagination.vue'

export const DfPagination = withInstall(Pagination)

export { paginationProps, paginationEmits, type PaginationProps } from '@df-ui/core'
