import { withInstall } from '@df-ui/core'
import Card from './src/card.vue'

export const DfCard = withInstall(Card)

export { cardProps, cardShadows, type CardProps, type CardShadow } from '@df-ui/core'
