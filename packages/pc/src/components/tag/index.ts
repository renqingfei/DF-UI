import { withInstall } from '@df-ui/core'
import Tag from './src/tag.vue'

export const DfTag = withInstall(Tag)

export { tagProps, tagEmits, tagTypes, tagVariants, type TagProps, type TagType, type TagVariant } from '@df-ui/core'
